export class DebugOptions {
    /// Log mouse position on map.
    ///
    /// The map logs every frame about the position of mouse on map, and the closest point from it to an end of a wall.
    static logMousePosition = false;

    /// Display the frame rate.
    static displayFPS = false;
}
